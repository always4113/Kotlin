package functions.declaration

//Kotlin's functions are declared using the fun keyword:
fun double(x: Int): Int {
  return 2 * x
}

// example code for calling member functions (usageOutline.kt)
class Stream {
  fun read(a: Int):Int {
    return 2 + a
  }
}